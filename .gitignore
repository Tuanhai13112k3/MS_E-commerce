# 1. Dependencies (Thư viện - QUAN TRỌNG NHẤT)
node_modules
.pnp
.pnp.js

# 2. Production Build (File đã build ra - Không đẩy lên git)
dist
dist-ssr
*.local

# 3. Environment Variables (BẢO MẬT - Chứa API Key, Password)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
# LƯU Ý: Giữ lại file ví dụ để người khác biết cần điền gì
!.env.example

# 4. Logs (File nhật ký lỗi)
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# 5. Editor & OS files (File rác của máy tính và VS Code)
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
Thumbs.db

# 6. Testing coverage (Nếu sau này có test)
coverage/

# 7. Docker (Nếu có file cấu hình đè của riêng máy local)
docker-compose.override.yml